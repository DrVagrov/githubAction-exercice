name : hello-fittrack
on: [pull_request]
jobs:
  build: 
      runs-on: ubuntu-latest
      steps:
        - name : echo
          run : echo ${{github.actor}}
  test: 
      needs: build
      runs-on: ubuntu-latest
      steps:
        - name : echo
          run : echo ${{github.actor}}
  
